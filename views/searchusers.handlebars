<h3 class="mt-3">Search User</h3>
{{#if error}} <span>{{error}}</span>{{/if}}
  <form class="d-flex col-6 " method="POST" action="/user/search">
    <input class="form-control me-sm-2" name="id" type="text" placeholder="Search user by email">
    <button class="btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
  </form>
